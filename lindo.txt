let camiao
function camiaoWindow() {
  nativeTheme.themeSource = 'light'
  const main = BrowserWindow.getFocusedWindow()
  if (main) {
    camiao = new BrowserWindow({
      width: 1010,
      height: 720,
      //autoHideMenuBar: true,
      resizable: false,
      parent: main,
      modal: true,
      webPreferences: {
        preload: path.join(__dirname, 'preload.js')
      }
    })
  }





     DataEntradaNota.value = nota.Dentradanota ? nota.Dentradanota.split('T')[0] : ""
    Dsaidanota.value = nota.Dsaidanota ? nota.Dsaidanota.split('T')[0] : ""   